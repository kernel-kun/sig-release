pr: 139045
pr_body: ""
releasenote:
  text: |-
    Fixed an issue where the `kubelet` would delete the CSI mount directory when a periodic `NodePublishVolume` call (triggered by setting `CSIDriver.spec.requiresRepublish` to true) returned an error, leaving the Pod with stale volume contents that subsequent successful republishes could not repair.
  sigs:
  - storage
  kinds:
  - bug
